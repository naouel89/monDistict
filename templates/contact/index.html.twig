

{% block body %}


		<h2 class="fst-italic text-white text-center">Nous contacter</h2>

		{# <div class="container mx-auto d-flex justify-content-center" id="formaulaire"> #}
				<div class="container mx-auto text-white mt-5" id="formulaire">

			{# <div class="row d-flex justify-content-center"> #}
			{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
			<div class="my-custom-class-for-errors">
				{{ form_errors(form) }}
			</div>

			<div class="row">
				<div
					class="col-md-6 mb-4">
					{# <div class="col-9 mx-auto mb-2 mt-4"> #}
					{{ form_row(form.nom) }}
				</div>

				{# <div class="col-9 mx-auto mb-2 mt-4"> #}
				<div class="col-md-6 mb-4">
					{{ form_row(form.prenom) }}
				</div>
			</div>
			<div
				class="row">
				{# <div class="col-9 mx-auto mb-2"> #}
				<div class="col-md-6 mb-4">
					{{ form_row(form.email) }}
				</div>

				{# <div class="col-9 mx-auto mb-2 mt-4"> #}
				<div class="col-md-6 mb-4">
					{{ form_row(form.telephone) }}
					{# {{ form_errors(form.objet) }} #}
				</div>
			</div>
			<div class="row">
				<div
					class="col-md-6 mb-4">
					{# <div class="col-9 mx-auto mb-2"> #}
					{{ form_row(form.message) }}
				</div>
			</div>
			{# </div> #}
            	<div class="row">
				<div
					class="col-md-6 mb-4">
					{# <div class="col-9 mx-auto mb-2"> #}
					{{ form_row(form.objet) }}
				</div>
			</div>
			

			<div class="my-custom-class-for-errors">
				{{ form_errors(form) }}
			</div>
			{{ form_end(form) }}
		</div>        
        


	</div>
	{# </div> #}

{% endblock %}